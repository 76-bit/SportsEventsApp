@model SportsEventsApp.Models.PaginatedListViewModel<SportsEventsApp.Models.FightViewModel>

<h1>All Fights</h1>

<div class="fights">
    @foreach (var fight in Model.Items)
    {
        <div class="fight-card">
            <img src="@fight.ImageUrl" alt="@fight.Title" />
            <h2>@fight.Title</h2>
            <p>@fight.Description</p>
            <p>@fight.DateOfTheFight.ToShortDateString()</p>
            <a asp-action="Details" asp-route-id="@fight.Id">View Details</a>
        </div>
    }
</div>

<div class="pagination">
    @if (Model.CurrentPage > 1)
    {
        <a asp-action="Index" asp-route-page="@(Model.CurrentPage - 1)">Previous</a>
    }
    @if (Model.CurrentPage < Model.TotalPages)
    {
        <a asp-action="Index" asp-route-page="@(Model.CurrentPage + 1)">Next</a>
    }
</div>
