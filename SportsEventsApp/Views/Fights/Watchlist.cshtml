@model SportsEventsApp.Models.PaginatedListViewModel<SportsEventsApp.Models.FightViewModel>

<h1>My Watchlist</h1>

<div class="fights">
    @foreach (var fight in Model.Items)
    {
        <div class="fight-card">
            <img src="@fight.ImageUrl" alt="@fight.Title" class="img-fluid" />
            <h2>@fight.Title</h2>
            <p>@fight.Description</p>
            <p>Date: @fight.DateOfTheFight.ToShortDateString()</p>
            <div class="card-actions">
                <!-- See Details Button -->
                <a asp-action="Details" asp-route-id="@fight.Id" class="btn btn-primary">See Details</a>

                <!-- Remove from Watchlist Button -->
                <form asp-action="RemoveFromWatchlist" asp-route-id="@fight.Id" method="post" class="d-inline">
                    <button type="submit" class="btn btn-danger">Remove from Watchlist</button>
                </form>
            </div>
        </div>
    }
</div>

<div class="pagination">
    @if (Model.CurrentPage > 1)
    {
        <a asp-action="Watchlist" asp-route-page="@(Model.CurrentPage - 1)" class="btn btn-secondary">Previous</a>
    }
    @if (Model.CurrentPage < Model.TotalPages)
    {
        <a asp-action="Watchlist" asp-route-page="@(Model.CurrentPage + 1)" class="btn btn-secondary">Next</a>
    }
</div>
